export type DiaryEntry = {
  id: string;
  date: string;
  title: string;
  subtitle: string;
  photos: string[]; // 4:3 ratio images
  paragraph: string;
  quote: string;
};

export const diaryEntries: DiaryEntry[] = [
  
];